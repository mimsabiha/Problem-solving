#include<bits/stdc++.h>
#define pb push_back
 vector < long long > a,b,c;
 int n;
 long long mx;

int sol(int n)
{

    if(n==0)
        return;

    else if(n>0)
            mx = max(a[n])

}


using namespace std;

int main()
{

    cin >> n;
    //vector < long long > a,b;
    for(int i=0;i<n;i++)
    {
        long long s;
        cin >> s;
        a.pb(s);
    }
    for(int i=0;i<n;i++)
    {
        long long s;
        cin >> s;
        b.pb(s);
    }


}
